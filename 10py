def reverseString(s):
    left, right = 0, len(s) - 1
    while left < right:
        s[left], s[right] = s[right], s[left]
        left += 1
        right -= 1
s = ['T', 'H', 'I', 'R', 'U','M','A','L','A','I']
reverseString(s)
print(s)  
